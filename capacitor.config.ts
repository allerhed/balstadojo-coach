import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.allerhed.balstadojo.coach',
  appName: 'balstadojo-coach',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
